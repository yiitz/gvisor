# gVisor

This branch is a synthetic branch, containing only Go sources, that is
compatible with standard Go tools. See the master branch for authoritative
sources and tests.


yiitz note: 远程也需要用这个分支，不用master分支。
